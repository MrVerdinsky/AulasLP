using System;

namespace Ex02
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Como podes, dentro do Visual " +
                "Studio 2017, inicializar um repositório Git " +
                "num projeto já existente (criado sem repositório " +
                "Git)? E usando o terminal?");
            Console.WriteLine();
            Console.WriteLine("R: Usando o comando 'git init'");
        }
    }
}
